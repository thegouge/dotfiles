# Home Server IP
# uncomment and set the IP when we set up the home server again
# BEEFY_IP=

# setting default editor
export EDITOR="nvim"

# Adding stuff to the path
export PATH="$PATH:/usr/local/go/bin"
